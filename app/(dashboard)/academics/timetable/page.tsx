import { ComingSoon } from "@/components/shared/coming-soon";

export default function TimetablePage() {
  return (
    <ComingSoon
      title="Timetable"
      description="Weekly class and teacher schedules."
      note="Built on Day 3: weekly timetable view, per-class and per-teacher views."
    />
  );
}
